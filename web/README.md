# Ostadi — واجهة الويب

التطبيق مبني بـ Vite + React + TypeScript. الإعداد الكامل للمشروع (Supabase، التشغيل، النشر) موثّق في [**README.md**](../README.md) في جذر المستودع.

```bash
cp .env.example .env
# عدّل VITE_SUPABASE_URL و VITE_SUPABASE_ANON_KEY
npm install
npm run dev
```
